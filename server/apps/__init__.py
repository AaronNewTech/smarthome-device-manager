"""Package for local Django apps.

This file exists so `server.apps` is a proper Python package and
local apps such as `apps.devices` can be imported by Django.
"""

# Intentionally left minimal. No runtime side-effects.
