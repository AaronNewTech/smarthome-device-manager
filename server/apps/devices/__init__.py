# devices app package

"""
Django "devices" app placeholder package.

This app will contain models, serializers, views, urls and tests
to provide a CRUD API for smart-home devices (the equivalent of
the Flask app in flask-sqlalchemy-server-backup/).
"""
